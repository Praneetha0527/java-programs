class Solution {
    public int maximumWealth(int[][] acc) {
        int max = 0;
        for (int[] arr:acc) {
            int sum=0;
            for (int a:arr) {
                sum+=a;
            }
            max=Math.max(max,sum);
        }
        return max;
    }
}
