class Solution {
    public int maximumGap(int[] nums) {
        int n=nums.length,res=0;
        if(n<2)return 0;
        else{
            Arrays.sort(nums);
            for(int i=1;i<n;i++){
                res=Math.max(res,nums[i]-nums[i-1]);
            }
        }return res;
    }
}
